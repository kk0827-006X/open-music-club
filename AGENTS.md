# Open Music Club Agent 规则

## 项目说明

Open Music Club 是一个面向受控成员、部署在公网 HTTPS 环境中的音乐共享与播放项目。

项目包含两类音乐来源：

1. 网易云接口音乐
2. 用户上传的本地音乐

两者必须保持独立，不要混在同一套数据逻辑里。

## 协作规则

- 全程使用中文进行对话和代码注释。
- 每次修改代码后，自行在终端运行测试；如果只能通过浏览器验证，则详细指导用户完成验证。
- 核心工程纪律：每轮只完成一个可独立验收的功能，不修改无关模块。如果预计涉及多个模块，先说明修改范围与影响，切忌随意重构无关代码。
- 每完成一个独立功能点并通过验证后，提醒用户进行一次 Git Commit。
- 用户是非计算机专业学生。遇到关键算法、数据结构或系统调用时，用一两句话解释底层原理。

## 开发规则

1. 后端统一使用 JavaScript + Node.js + Express。

2. 包管理统一使用 pnpm，不要使用 npm 或 yarn 修改依赖。

3. Open Music Club 自己的业务代码放在 `src/` 下。

4. `vendor/api-enhanced/` 属于第三方网易云接口代码，尽量不要直接修改。优先通过配置、白名单或封装的方式调用。

5. 网易云接口只保留基础音乐功能，例如：

   - 用户信息
   - 用户歌单
   - 播放记录
   - 歌曲搜索
   - 歌曲详情
   - 播放地址
   - 歌词
   - 歌手
   - 专辑

   不需要评论、排行榜、推荐、FM、直播、动态等功能。

6. Open Music Club 账号和网易云账号必须分开。网站登录使用邮箱、密码和 Session。

7. 密码必须使用 bcrypt 加密保存，禁止保存明文密码。

8. 用户上传的音乐文件保存在 `storage/music/`，数据库只保存文件信息和路径，不保存音乐二进制文件。

9. 所有需要登录的接口必须在后端检查权限。管理员接口必须额外检查 admin 权限。

10. 不要随意大规模重构或删除文件。修改功能时只改与当前任务相关的代码。

11. 生产环境通过 Caddy、Nginx 等反向代理终止 HTTPS。Node.js 服务默认只监听回环地址或受保护的容器网络，不直接暴露到公网。

12. 公网部署必须使用 HTTPS；生产环境不得提供可继续使用的明文 HTTP 业务入口，HTTP 只允许重定向到 HTTPS。

13. 登录认证需要支持基于认证器 App 的 TOTP 2FA。管理员账号在公网正式开放前必须启用 2FA；普通用户允许逐步启用。2FA 密钥必须加密保存，恢复码只能保存哈希。

14. 不要把反向代理传入的客户端 IP、协议或 Host 默认视为可信。Express `trust proxy` 必须按实际代理层数精确配置，禁止无条件信任任意代理。

## 安全规则

- `.env`、数据库文件、用户上传音乐不得提交到 Git。
- 生产 Session Cookie 必须启用 `secure`、`httpOnly` 和合适的 `sameSite`；登录和 2FA 成功后必须重新生成 Session ID。
- 所有修改数据的接口必须有 CSRF 防护；CORS 默认只允许正式站点同源访问，不使用任意来源通配符。
- 登录、2FA、访问申请、上传、下载和网易云代理接口必须设置与风险匹配的速率限制。
- 上传文件必须限制格式、MIME、文件签名、大小和数量，并通过元数据解析复核；上传目录必须不可执行、不可被静态目录直接访问。
- 上传临时文件、音乐文件或封面在校验、数据库写入或删除事务失败时必须清理，防止孤儿文件和磁盘耗尽。
- 不允许通过静态目录直接暴露音乐文件。
- 网易云解灰功能默认关闭。
- 公网响应不得包含堆栈、数据库路径、内部文件路径、环境变量、Session Secret、2FA 密钥或上游服务内部错误。
- 服务端日志可以记录内部错误和请求 ID，但必须脱敏密码、Cookie、Session ID、TOTP、恢复码和网易云凭据。
- 对外响应需要配置基础安全响应头，包括 HSTS、CSP、`X-Content-Type-Options: nosniff`、点击劫持防护和合理的 Referrer Policy。
- 数据库、Session 文件、`storage/`、管理端口和 Node.js 监听端口不得直接暴露到公网；防火墙只开放必要端口。
- 依赖升级或新增依赖后使用 pnpm 执行安全审计；发现高危漏洞时明确记录风险，不得静默忽略。

## 完成任务前

修改完成后至少检查：

- 服务是否能正常启动
- 当前功能是否可用
- 是否影响已有功能
- 是否新增依赖或环境变量
- 公网错误响应是否完成脱敏
- HTTPS、Secure Cookie、CSRF、CORS、速率限制和反向代理信任配置是否符合生产环境要求
- 上传目录、数据库、Session Store 和内部服务是否仍未直接暴露
- 2FA 是否覆盖启用、验证、恢复码、重放/暴力尝试限制和管理员强制策略

如果没有测试，不要直接声明“已经完成”。
