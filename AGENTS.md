# Open Music Club Agent 规则

## 项目说明

Open Music Club 是一个局域网音乐共享与播放项目。

项目包含两类音乐来源：

1. 网易云接口音乐
2. 用户上传的本地音乐

两者必须保持独立，不要混在同一套数据逻辑里。

## 协作规则

- 全程使用中文进行对话和代码注释。
- 每次修改代码后，自行在终端运行测试；如果只能通过浏览器验证，则详细指导用户完成验证。
- 核心工程纪律：每轮只完成一个可独立验收的功能，不修改无关模块。如果预计涉及多个模块，先说明修改范围与影响，切忌随意重构无关代码。
- 每完成一个独立功能点并通过验证后，提醒用户进行一次 Git Commit。
- 用户是非计算机专业学生。遇到关键算法、数据结构或系统调用时，用一两句话解释底层原理。

## 开发规则

1. 后端统一使用 JavaScript + Node.js + Express。

2. 包管理统一使用 pnpm，不要使用 npm 或 yarn 修改依赖。

3. Open Music Club 自己的业务代码放在 `src/` 下。

4. `vendor/api-enhanced/` 属于第三方网易云接口代码，尽量不要直接修改。优先通过配置、白名单或封装的方式调用。

5. 网易云接口只保留基础音乐功能，例如：

   - 用户信息
   - 用户歌单
   - 播放记录
   - 歌曲搜索
   - 歌曲详情
   - 播放地址
   - 歌词
   - 歌手
   - 专辑

   不需要评论、排行榜、推荐、FM、直播、动态等功能。

6. Open Music Club 账号和网易云账号必须分开。网站登录使用邮箱、密码和 Session。

7. 密码必须使用 bcrypt 加密保存，禁止保存明文密码。

8. 用户上传的音乐文件保存在 `storage/music/`，数据库只保存文件信息和路径，不保存音乐二进制文件。

9. 所有需要登录的接口必须在后端检查权限。管理员接口必须额外检查 admin 权限。

10. 不要随意大规模重构或删除文件。修改功能时只改与当前任务相关的代码。

## 安全规则

- `.env`、数据库文件、用户上传音乐不得提交到 Git。
- 上传文件必须限制格式和大小。
- 不允许通过静态目录直接暴露音乐文件。
- 网易云解灰功能默认关闭。

## 完成任务前

修改完成后至少检查：

- 服务是否能正常启动
- 当前功能是否可用
- 是否影响已有功能
- 是否新增依赖或环境变量

如果没有测试，不要直接声明“已经完成”。
